[workspace]
resolver = "2"
# `host` builds for the native target; plugins build for wasm32 and are
# compiled separately (they import `host.*`, so they cannot link natively).
default-members = ["host"]
members = ["host", "plugins/hello-rust", "plugins/hello-rust-v2", "plugins/plain-rust"]
