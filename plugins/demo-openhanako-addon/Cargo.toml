[package]
name = "demo-openhanako-addon"
version = "0.1.0"
edition = "2021"

# Second plugin that extends openhanako-shell through its public slots only.

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk = { path = "../../plugin-sdk" }
serde_json = "1"
