# demo-openhanako-addon

Example WASM plugin that fills `openhanako-shell` slots with sample components.

Covers titlebar / sidebar / conversation / preview / rail / shell.overlay.
Only depends on public `openhanako.*` slot names.
