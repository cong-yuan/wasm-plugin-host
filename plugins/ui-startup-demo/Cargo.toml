[package]
name = "ui-startup-demo"
version = "0.1.0"
edition = "2021"

# Demonstrates `open: "startup"`: this plugin owns the app's launch view, so
# the app opens *its* window at startup instead of the default page.

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
